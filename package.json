{
  "name": "pastapresto",
  "version": "0.1.0",
  "private": true,
  "description": "PastaPresto! - QR-to-kitchen pasta ordering. Static PWA on GitHub Pages / Firebase Hosting, Firestore backend.",
  "scripts": {
    "test": "node scripts/selftest.js && node scripts/qr-verify.js",
    "selftest": "node scripts/selftest.js",
    "qr": "node scripts/qr-verify.js",
    "serve": "node scripts/dev-serve.js",
    "rules": "firebase deploy --only firestore:rules"
  },
  "engines": {
    "node": ">=18"
  },
  "type": "module"
}
